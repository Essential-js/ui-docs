import { Header } from '@essential-js/ui/docs/header';
import { CopyableCode } from '@essential-js/ui/code';
import { Textarea } from '@essential-js/ui/form';

<Header pretitle="Form" title="Texarea" />

<CopyableCode>{`import {Texarea} from '@essential-js/ui/form';`}</CopyableCode>

<section>
	<h3>Default types</h3>

    <Textarea label="Insert your comment" placeholder="Awesome comment!" />
    <Textarea disabled value="Im disabled!" label="Insert your comment" placeholder="Awesome comment!" />

</section>

<CopyableCode>
{`
import React from 'react';
import {Input} from '@essential-js/ui/form';

function App() {
const [values, setValues] = React.useState({disabled: 'Im disabled!', regular: ''});

        function handleChanges(event) {
            const {value, name} = event.target;
            setValues({...values, [name]: value})
        }

        return (
            <>
                <Textarea
                    onChange={handleChanges}
                    label="Insert your comment"
                    value={values.regular}
                    placeholder="Awesome comment!"
                />

                <Textarea
                    onChange={handleChanges}
                    disabled
                    value={values.disabled}
                    label="Insert your comment"
                    placeholder="Awesome comment!"
                />
            </>
        )
    }

`}

</CopyableCode>

<section>
	<h3>Bordered types</h3>

    <Textarea className="bordered success" label="Insert your comment" placeholder="Awesome comment!" />
    <Textarea className="bordered error" label="Insert your comment" placeholder="Awesome comment!" />
    <Textarea className="bordered warning" label="Insert your comment" placeholder="Awesome comment!" />
    <Textarea className="bordered info" label="Insert your comment" placeholder="Awesome comment!" />

</section>

<CopyableCode>
{`
import React from 'react';
import {Input} from '@essential-js/ui/form';

const INITIAL_STATE = {success: '', error: '', warning: '', info: ''};

function App() {
const [values, setValues] = React.useState(INITIAL_STATE);

        function handleChanges(event) {
            const {value, name} = event.target;
            setValues({...values, [name]: value})
        }

        return (
           <>
             <Textarea
                onChange={handleChanges}
                name="success"
                value={values.success}
                className="bordered success"
                label="Insert your comment"
                placeholder="Awesome comment!"
             />

            <Textarea
                onChange={handleChanges}
                name="error"
                value={values.error}
                className="bordered error"
                label="Insert your comment"
                placeholder="Awesome comment!"
             />

            <Textarea
                onChange={handleChanges}
                name="warning"
                value={values.warning}
                className="bordered warning"
                label="Insert your comment"
                placeholder="Awesome comment!"
             />

            <Textarea
                onChange={handleChanges}
                name="info"
                value={values.info}
                className="bordered info"
                label="Insert your comment"
                placeholder="Awesome comment!"
             />

           </>
        )
    }

`}

</CopyableCode>
