import { Header } from '@essential-js/ui-docs/header';
import { CopyableCode } from '@essential-js/ui/code';

<Header pretitle="Introduction" title="Getting Started" />

To start using essential-ui components you only have to install a package in your project, just that, no style packs, no weird stuff, that's all.

<section>
	<CopyableCode>npm i @essential-js/ui</CopyableCode>
</section>

We are now ready to start using Essential UI components.

<h3></h3>
