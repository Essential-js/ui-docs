import * as React from 'react';
import { Header } from '../components/header';

export /*bundle*/
function View({ store }) {
	console.log(store);
	return (
		<div className="">
			<Header />
		</div>
	);
}
